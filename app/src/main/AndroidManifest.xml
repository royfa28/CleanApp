<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cleanapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TenantHomeActivity"></activity>
        <activity android:name=".RoomDetailModal" />
        <activity android:name=".HouseInvitation" />
        <activity android:name=".TenantList" />
        <activity android:name=".HouseDetailsActivity" />
        <activity android:name=".SetuserlvlActivity" />
        <activity android:name=".AddHouseActivity" />
        <activity android:name=".OwnerMainActivity" />
        <activity android:name=".TenantMainFragment" />
        <activity android:name=".registerActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>